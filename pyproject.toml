[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "zero-tolerance-system"
version = "1.0.0"
description = "Zero Tolerance Python Contract Enforcer with MCP Server"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=0.9.0",
    "mcp>=1.0.0",
    "pyyaml>=6.0",
    "openai>=1.0.0",
    "requests>=2.28.0",
    "colorama>=0.4.0",
    "tqdm>=4.64.0",
    "radon>=5.1.0",
    "watchdog>=3.0.0",
]

[tool.setuptools]
py-modules = []

[tool.setuptools.packages.find]
where = ["."]
include = ["enforcement*", "app*"]
exclude = ["zt*", "logs*", "data*", "hooks*", "project*", "tests*"]
